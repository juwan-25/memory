<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="Post_Gallery.css">
    <link rel="stylesheet" href="Post_Gallery_Mobile.css">
    <!-- Pretendard 폰트 -->
    <link rel="stylesheet" type="text/css"
        href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard-dynamic-subset.css" />
    <!-- 스와이퍼 -->
    <link rel="stylesheet" href="https://unpkg.com/swiper/swiper-bundle.min.css">
    <!-- nav -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <!-- jQuery -->
    <script type="text/javascript" src="https://code.jquery.com/jquery-1.10.2.min.js"></script>

    <title>게시물 나열 페이지</title>
</head>

<body style="overflow-x:hidden">
    <nav class="navbar navbar-expand-lg bg-white">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Navbar</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
                aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                    <a class="nav-link" href="#">HOME</a>
                    <a class="nav-link" href="#">ABOUT</a>
                    <a class="nav-link" href="gallery">MEMORY</a>
                    <a class="nav-link" href="guest">COMMENT</a>
                </div>
            </div>
        </div>
    </nav>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous">
    </script>

    <div class="line"></div>
    <p class="rankingTitle">BEST MEMORY</p>

    <div class="swiper-container" id="ranking">
        <ul class="swiper-wrapper" id="slides">
            <% for(let i=0; i < data.length; i++) { %>
                <li class="swiper-slide">
                    <a href="">
                        <div class="numberRectangle">
                            <%= i + 1 %>
                        </div>
                        <img src="<%= data[i].mimage %>" alt=""/>
                        <div class="preview">
                            <p class="title"><%= data[i].title %></p>
                            <p class="detail"><%= data[i].content %></p>
                        </div>
                    </a>
                </li>
                <% } %>
        </ul>
    </div>

    <script src="https://unpkg.com/swiper/swiper-bundle.min.js"></script>
    <script>
        //화면 크기 변경 시 새로고침 해야 됨
        new Swiper('#ranking', {
            slidesPerView: window.innerWidth <= 390 ? 1.17 : 4,
            freeMode: true, // 스냅 이동 효과 비활성화
            pagination: {
                clickable: true,
            }
        });


    </script>

    <div class="galleryTitle">· 기억 저장소 ·</div>
    <div class="postGallery">
        <script>
            const galleryContainer = document.querySelector('.postGallery');

            if (window.innerWidth <= 390) {
                for (let i = 1; i <= 23; i++) {  //배열로
                    if (i % 3 == 0) {
                        const itemLarge = document.createElement('div');
                        itemLarge.classList.add('itemLarge');

                        itemLarge.innerHTML = `
                      <a href="">
                        <figure>
                          <img src="" alt="">
                          <figcaption>
                            <div class="title">생애 첫 비행기의 기억</div>
                            <div class="detail">그 때는 제가 처음 가족들과 여행을 갔던 때였어요.<br>
                              번아웃이 와서 정말 힘들었었는데, 언니가 직접 표를 끊고
                              같이 여행이나 가자고 한 게 시작이었어요...</div>
                          </figcaption>
                        </figure>
                      </a>
                    `;
                        galleryContainer.appendChild(itemLarge);
                    } else {
                        const item = document.createElement('div');
                        item.classList.add('item');

                        item.innerHTML = `
                      <a href="">
                        <figure>
                          <img src="" alt="">
                          <figcaption>
                            <div class="title">생애 첫 비행기의 기억</div>
                            <div class="detail">그 때는 제가 처음 가족들과 여행을 갔던 때였어요.<br>
                              번아웃이 와서 정말 힘들었었는데, 언니가 직접 표를 끊고
                              같이 여행이나 가자고 한 게 시작이었어요...</div>
                          </figcaption>
                        </figure>
                      </a>
                    `;
                        galleryContainer.appendChild(item);
                    }
                }
            } else {
                let index = -4;
                let plus = 6;

                for (let i = 1; i <= 22; i++) {  //배열로
                    if (i == index + plus) {
                        if (plus == 6) {
                            plus = 4;
                        } else if (plus == 4) {
                            plus = 6;
                        }

                        const itemLarge = document.createElement('div');
                        itemLarge.classList.add('itemLarge');

                        itemLarge.innerHTML = `
                      <a href="">
                        <figure>
                          <img src="" alt="">
                          <figcaption>
                            <div class="title">생애 첫 비행기의 기억</div>
                            <div class="detail">그 때는 제가 처음 가족들과 여행을 갔던 때였어요.<br>
                              번아웃이 와서 정말 힘들었었는데, 언니가 직접 표를 끊고
                              같이 여행이나 가자고 한 게 시작이었어요...</div>
                          </figcaption>
                        </figure>
                      </a>
                    `;

                        galleryContainer.appendChild(itemLarge);

                        index = i;
                    } else {
                        const item = document.createElement('div');
                        item.classList.add('item');

                        item.innerHTML = `
                      <a href="">
                        <figure>
                          <img src="" alt="">
                          <figcaption>
                            <div class="title">생애 첫 비행기의 기억</div>
                            <div class="detail">그 때는 제가 처음 가족들과 여행을 갔던 때였어요.<br>
                              번아웃이 와서 정말 힘들었었는데, 언니가 직접 표를 끊고
                              같이 여행이나 가자고 한 게 시작이었어요...</div>
                          </figcaption>
                        </figure>
                      </a>
                    `;

                        galleryContainer.appendChild(item);
                    }
                }
            }

            var status; //추가할 개수 임시 판별 변수
            var add = 0;    //index에 추가할 개수
            var totalElements = $(".postGallery>div").length;   //div 총 개수

            if (window.innerWidth <= 390) {
                index = 8;
                status = 1;
            } else {
                index = 7;
                status = 0;
            }
            $(document).ready(function () {
                $(".postGallery>div").hide();   //div를 모두 숨김
                $(".postGallery>div").slice(0, index).show();   //처음에 0번째부터 6번째까지 보여줌

                //div 개수가 index보다 적거나 같으면 더보기 버튼 안 보이게
                if (totalElements <= index) {
                    $(".more").hide();
                }

            })

            function more() {
                if (status == 0) {
                    add = 8;
                    status = 1;
                } else if (status == 1) {
                    add = 7;
                    status = 0;
                }

                $(".postGallery>div").slice(index, index + add).show();

                index += add;

                //div 개수가 index보다 적거나 같으면 더보기 버튼 안 보이게
                if (index >= totalElements) {
                    $(".more").hide();
                }
            }

        </script>
        <button class="more" onclick="more()">더보기</button>
    </div>
    <footer id="footer"></footer>
</body>

</html>